; Keep lockfile generation consistent with CI, which uses npm defaults.
; A global `legacy-peer-deps=true` omits optional peer deps (e.g. @types/node)
; from package-lock.json and breaks `npm ci`.
legacy-peer-deps=false
